rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for authenticated users
    // In production, you should implement proper security rules
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Specific rules for vehicle_photos collection
    match /vehicle_photos/{photoId} {
      allow read, write: if true;
    }
    
    // Specific rules for vehicles collection
    match /vehicles/{vehicleId} {
      allow read, write: if true;
    }
    
    // Specific rules for service_records collection
    match /service_records/{recordId} {
      allow read, write: if true;
    }
  }
}
